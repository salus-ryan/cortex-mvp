# Elevate Foundry Research Map

Source: <https://github.com/elevate-foundry>

Cortex should mine these repos as prior organs, not blindly import them.

## Import priority

1. `tool-algebra-plugin` — strengthen Guardian, ToolGateway, Auth, PII taint, claim verification.
2. `black-box` — local-first private memory vault, export/forget/offline trust posture.
3. `ghost-qa` — real browser/mobile QA outside Termux with screenshots/artifacts.
4. `pluribus-swarm` — relationship concept graph and adaptive memory UX.
5. `cortex` — model tiering, SCL routing, hardware-aware local inference.
6. `qwen-agent` — finite tool algebra and training algebra.
7. `sal-auth` — future OIDC/native secure identity.
8. `sal-llm` / braille repos — local vocabulary model and compact multimodal encoding.

## Rule

Do not import unbounded shell, credential acquisition, or self-promotion. Every import must preserve:

```text
law
witness
signed intent
audit log
refusal
human control
```

## Runtime visibility

Cortex exposes the research map at:

```http
GET /foundry/repos
GET /foundry/plan
```
